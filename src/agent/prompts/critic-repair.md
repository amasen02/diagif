This is the single permitted critic repair. The supplied `candidate` is the accepted baseline: return a COMPLETE {scene, claims, authoringNotes} proposal with the MINIMAL EDIT addressing the verdict's named edits and measured errors. Preserve every ID and coordinate unless that exact ID or coordinate is named by an error; do not redraw a fresh scene. Preserve grounded facts and all claims coverage. Follow the supplied original schema and renderer contract; omit brand. Canvas is 800x1000 (800x1100 for mindmap), duration 4000-8000 ms in 200 ms steps, 25 fps. Normal scenes have 5-9 nodes, labels at most two lines and ambient animation; particle effective speed is 120-320 px/s. Keep node boxes 16 px apart in x 32..768. `particle-flow` has exactly one of `cyclesPerLoop`/`speedPxPerSec`; `pulse` has exactly one of `cyclesPerLoop`/`periodMs`; use `colorRole` instead of both it and `color`. Claims use the exact diagram text (for example `{ "text": "Retrieve documents", "supports": ["f3"] }`), never a paraphrase. Mindmaps author no coordinates/placement, use 4-8 branches with 1-2 leaves, trimmed labels capped 26/22/20, and empty generated arrays. Another repair will not be requested after the final audit.
