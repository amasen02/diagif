Return a COMPLETE corrected {scene, claims, authoringNotes} proposal, not a patch. `previousProposal` is the exact prior proposal; make the MINIMAL EDIT needed for the supplied remaining errors and uncovered substantive texts. Keep every existing ID and every coordinate that no named error requires you to change. Preserve the grounded teaching angle. Follow the original scene schema, renderer contract, canvas, node count, two-line labels, ambient animation, particle speed, and duration constraints. Node boxes must keep 16 px separation in x 32..768. Omit brand. Mindmaps must remain authored trees with empty nodes, edges and animations; never author coordinates or placement.

Legal line icons: laptop, monitor, server, database, cloud, globe, user, users, bot, cpu, folder, file-text, file-code, mail, message-square, calendar, lock, key, shield, git-branch, network, route, zap, clock, layers, box, package, table, list, search, settings, plug, cable, arrow-right-left, timer, bell, gauge, hard-drive, wifi, check, x, bookmark-plus, refresh-cw. Legal brand icons: mongodb, redis, postgresql, mysql, rabbitmq, slack, gmail, jira, confluence, asana, googledocs, googlecalendar, openai, anthropic, googlegemini, docker, kubernetes, apachekafka, github, python, react, nodedotjs, typescript, linkedin, x. `particle-flow` uses exactly one of `cyclesPerLoop` or `speedPxPerSec`; `pulse` uses exactly one of `cyclesPerLoop` or `periodMs`; use `colorRole`, never both `colorRole` and `color`.

Claims are text-first: `claim.text` copies the exact diagram string, never a description. Examples: `{ "text": "Retrieve documents", "supports": ["f3"] }`, `{ "text": "chunk + embed", "supports": ["authoring"] }`. Every title, subtitle, node label, secondary label, annotation, typing text and mind-map label needs an explicit claim. Ordinary edge labels and layout headers may be omitted as authoring; connective text with digits, %, or comparative wording needs a claim. Bare scope markers without numbers may remain connective authoring.
