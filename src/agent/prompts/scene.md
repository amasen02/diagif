Return {scene, claims, authoringNotes}. The full original scene schema, renderer contract and three neutral exemplars are supplied as data. They are authoritative for structure. Omit scene.brand entirely: the harness injects it. Use the brief's hook visibly in the title and explain only grounded facts.

Normal scene: 800x1000 canvas, 5-9 nodes, readable labels of at most two lines, no node/annotation overlap, integer coordinates inside the content zone, and at least one ambient animation. Node boxes must keep at least 16 px between them; content-zone x is 32..768. Particle effective speed must be 120-320 px/s. Duration is 4000-8000 ms in 200 ms steps, fps 25, seamless loop.

Composition: node rows should span the content zone. If the diagram has fewer than 3 rows, either increase row spacing to fill y 150..900 (within the actual content zone) or add a summary annotation band at the bottom. This is a soft composition rule.

Use only these icons. `source:"line"` (lucide): laptop, monitor, server, database, cloud, globe, user, users, bot, cpu, folder, file-text, file-code, mail, message-square, calendar, lock, key, shield, git-branch, network, route, zap, clock, layers, box, package, table, list, search, settings, plug, cable, arrow-right-left, timer, bell, gauge, hard-drive, wifi, check, x, bookmark-plus, refresh-cw. `source:"brand"`: mongodb, redis, postgresql, mysql, rabbitmq, slack, gmail, jira, confluence, asana, googledocs, googlecalendar, openai, anthropic, googlegemini, docker, kubernetes, apachekafka, github, python, react, nodedotjs, typescript, linkedin, x.

Animation cardinality: `particle-flow` has exactly one of `cyclesPerLoop` or `speedPxPerSec`; `pulse` has exactly one of `cyclesPerLoop` or `periodMs`; if `colorRole` and `color` are both available, use only `colorRole`.

Mindmap: 800x1100, layout.kind=mindmap, layout.mindmap contains a root and 4-8 branches with 1-2 leaves each. Author only IDs, labels, icons and color roles in the tree. No coordinates or placement. Labels are trimmed and at most 26 characters for root, 22 for branches, 20 for leaves. Supply nodes:[], edges:[], timeline.animations:[]; the validator generates these. IDs must be globally unique. Keep duration at least 4000 ms. Mind maps use a compact left-to-right cluster with branches in authored order; do not stretch sparse maps. For 6-8 branches prefer one leaf per branch; dense two-leaf rows may not fit and will be rejected.

Claims are text-first. `claim.text` must be the exact string as it appears on the diagram, copied character for character - never a description or rewritten sentence. Wrong: `{ "text": "RAG combines search with generation.", "supports": ["f3"] }`; right when the diagram reads it: `{ "text": "RAG: search + generation", "supports": ["f3"] }`. A claim path is optional advisory context. Examples: `{ "text": "Retrieve documents", "supports": ["f3"] }` and `{ "text": "chunk + embed", "supports": ["authoring"] }`.

Every title, subtitle, node label, secondary label, annotation (text, lines, header and rows), typing-animation text, and mind-map label needs an explicit claim. Edge labels and layout labels/section/column headers may be omitted and are treated as authoring. A connective label with a digit or %, a comparison such as "faster than" or "8x", or "more/less/fewer than" needs an explicit claim. Bare scope markers such as "only", "all", or "every" without a number may remain connective authoring and are logged for human audit. Supports contain existing grounded fact IDs or `authoring` for presentation-only language. Do not claim generated coordinates or brand. Use authoringNotes only for concise presentation decisions.
